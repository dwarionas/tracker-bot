# 🚀 Покрокова інструкція з деплою на Vercel

## Швидкий старт

### 1️⃣ Деплой на Vercel

**Варіант A: Через Vercel CLI**
```bash
npm i -g vercel
vercel login
vercel
```

**Варіант B: Через GitHub**
1. Завантажте код на GitHub
2. На [vercel.com](https://vercel.com) → Add New Project → Import
3. Виберіть ваш репозиторій

### 2️⃣ Отримайте URL вашого проєкту

Після деплою ви побачите щось таке:
```
✅ Production: https://your-project-name.vercel.app
```

**Це і є ваш Webhook URL!** Скопіюйте його.

### 3️⃣ Додайте змінні оточення в Vercel

1. Перейдіть на [vercel.com](https://vercel.com)
2. Виберіть ваш проєкт
3. Settings → Environment Variables
4. Додайте:
   - **Key:** `BOT_KEY`
   - **Value:** ваш Telegram Bot Token (отримайте від @BotFather)

### 4️⃣ Встановіть Webhook (ЛОКАЛЬНО на вашому комп'ютері!)

**⚠️ ВАЖЛИВО:** Цю команду запускайте на **вашому комп'ютері**, НЕ на Vercel!

1. Відкрийте файл `.env` в корені проєкту:
```env
BOT_KEY=your_telegram_bot_token_here
WEBHOOK_URL=https://your-project-name.vercel.app
```

2. Замініть `your-project-name.vercel.app` на реальний URL з Vercel

3. В терміналі запустіть:
```bash
npm run set-webhook
```

4. Якщо все добре, ви побачите:
```
✅ Webhook успішно встановлено!
```

### 5️⃣ Перевірте роботу бота

Відкрийте вашого бота в Telegram і надішліть команду `/start`

---

## ❓ Часті питання

### Де знайти URL проєкту на Vercel?

1. Vercel Dashboard → ваш проєкт → Settings → Domains
2. Або Vercel Dashboard → ваш проєкт → Deployments → останній деплой

### Чи потрібно додавати WEBHOOK_URL в Vercel?

**НІ!** `WEBHOOK_URL` потрібна тільки локально в `.env` для скрипта `set-webhook`. В Vercel додавайте тільки `BOT_KEY`.

### Де запускати `npm run set-webhook`?

**На вашому комп'ютері**, в терміналі, в папці проєкту. НЕ на Vercel!

### Як перевірити, чи встановлено webhook?

Відкрийте в браузері:
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo
```

Замініть `<YOUR_BOT_TOKEN>` на ваш токен.

### Бот не відповідає після деплою

1. Перевірте, чи встановлено `BOT_KEY` в Vercel
2. Перевірте, чи встановлено webhook (див. вище)
3. Перевірте логи в Vercel Dashboard → ваш проєкт → Deployments → Functions

---

## 🔄 Оновлення webhook після зміни URL

Якщо ви змінили URL проєкту (наприклад, додали кастомний домен):

1. Оновіть `WEBHOOK_URL` в `.env`
2. Запустіть `npm run set-webhook` локально

